<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_permission">

    <!--    старт происходит с PermissionFragment, который запрашивает необходимые права  -->

    <fragment
        android:id="@+id/nav_map"
        android:name="ru.piteravto.osmroutetest.map.MapFragment"
        android:label="@string/menu_main_screen"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/nav_permission"
        android:name="ru.piteravto.osmroutetest.permission.PermissionFragment"
        android:label="@string/menu_permission"
        tools:layout="@layout/fragment_permission" />

</navigation>